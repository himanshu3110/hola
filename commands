some important commands:

add a branch to github
git push origin branch_name

deleting a branch from github
git push origin :branch_name

deleting a branch in terminal
git branch -d branch_name

a branch is added at github.com and to show this at terminal
git checkout -b branch_name

gpg --gen-key 

setting up alias:-
git config --global alias.co checkout

creating a tag:- git tag tag_name
pushing a tag:- git push origin tag_name

